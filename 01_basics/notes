# Add file git to track
git add nonexistent_file

# Add modified file to git
git add modified_file

# Use wildcards to add multiple files
git add * (careful)
git add *.c

# Revert a file from the staging area
git add file
git reset HEAD file

# Undo changes from the modified file
git checkout -- file
