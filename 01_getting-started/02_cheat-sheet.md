# Creating Snapshots

## Initializing a repository
git init <dir>

## Staging files
git add <filename>

or use wildcards like

git add *
git add .c

## Viewing the git repository status
git status 
git status -s

## Commiting the staged files

git commit -m "Commit message"
git commit # Opens the default editor to type long commit messages.
